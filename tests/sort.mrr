variables:
    {0..10}[10] tab = (3, 6, 8, 2, 4, 5, 7, 9, 0, 1): data to sort
    {0..10} tmp = 0: swap variable
rules:
    # NOTE: GAL does not support var swap without a third one
    tab[i]>tab[j] >> tmp=tab[i], tab[i]=tab[j], tab[j]=tmp for any i,j if j>i
